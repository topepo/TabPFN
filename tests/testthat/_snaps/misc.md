# data constraints

    i There are 10,283 rows in the training set.
    i TabPFN (version 2) is intended for training set sizes <= 10,000 rows.
    i Consider setting the option `ignore_pretraining_limits` to "TRUE" or subset the training size using the `training_set_limit` argument.

